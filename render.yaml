services:
  - type: web
    name: academic-integrity-api
    env: python
    buildCommand: "cd backend && pip install --upgrade pip setuptools wheel && pip install -r requirements-render.txt"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.13"
             
